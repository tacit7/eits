defmodule EyeInTheSkyWeb.DmLive.CheckpointHelpers do
  @moduledoc """
  Helpers for checkpoint operations in the DM LiveView.

  This module provides utilities for managing session checkpoints, including
  save, restore, and checkpoint-related UI state management. Checkpoint
  functionality integrates with the EyeInTheSky.Checkpoints context.
  """

  # Future: add checkpoint helpers here as checkpoint UI is developed
  # Examples:
  # - assign_checkpoint_defaults/1
  # - handle_create_checkpoint/3
  # - handle_restore_checkpoint/2
  # - handle_fork_checkpoint/2
end
